---
id: ""
parent_module: ""
depends_on: []
план читать: "нет"
status: "pending"
---

# Задача: <короткое название>

## Что сделать
Одно-два предложения — **измеримый результат** (файл, эндпоинт, модель), не процесс.

## Файлы
**Создать:**
- 

**Изменить:**
- 

**Не трогать без явного указания:** всё остальное.

## Контракт (если задача создаёт/меняет сущность)
Поля, типы, инварианты — только то, что нужно знать потребителю **после** этой задачи.
На границе модуля допустима **одна** сигнатура публичной factory/API, если без неё
сломается потребитель (редко).
Если задача не вводит новый контракт — N/A.

## Решения / якоря реализации
Короткий список **обязательных** решений для этой задачи — не код, не сигнатуры.

Когда заполнять:
- есть аналог в репо → путь к файлу/модулю-образцу;
- неочевидный порядок (lifespan, wire router, ensure_indexes);
- новый домен без образца (cursor-sdk, MCP) → что прочитать / какой API слоя.

Когда N/A:
- задача тривиальна (scaffold, env-example);
- паттерн полностью очевиден из `depends_on`.

Формат (0–5 bullet):
- Повторить паттерн: `src/.../X.py` — <что именно>.
- Обязательно: <одно архитектурное решение>.
- Не делать: <граница задачи>.

Запрещено: полные функции, SQL, тела тестов, copy-paste из плана модуля.

## Зависимости
Если `depends_on` не пуст — что именно берётся из этих задач (какие файлы/сущности),
не пересказывать их план целиком.

## Тесты
| Сценарий | Путь теста | Критерий |
|----------|-----------|----------|
| | | |

## DoD
- [ ] 
- [ ] Тесты из раздела «Тесты» зелёные
- [ ] Нет нарушений инвариантов проекта (актуальный список — `memory.md`, раздел «Инварианты»)
